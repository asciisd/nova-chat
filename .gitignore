/vendor/
/node_modules/
/.idea/
/.vscode/
/.phpunit.cache/
composer.lock
package-lock.json
.DS_Store
.phpunit.result.cache

# NOTE: /dist/ is intentionally NOT ignored. The compiled Vue bundle
# (dist/js/tool.js + tool.css) is what Nova::script / Nova::style
# load at runtime in consuming apps, so it must ship in the tagged
# release. Rebuild with `npm run build` before tagging.
